{
  "version": 2,
  "buildCommand": "prisma generate && DATABASE_URL=$DATABASE_URL prisma migrate deploy && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": ".next",
  "regions": ["hnd1"],
  "env": {
    "NEXTAUTH_URL": "https://next-todo-app-umber.vercel.app",
    "DATABASE_URL": "${DATABASE_URL}",
    "NEXTAUTH_SECRET": "${NEXTAUTH_SECRET}"
  },
  "build": {
    "env": {
      "NEXTAUTH_URL": "https://next-todo-app-umber.vercel.app",
      "DATABASE_URL": "${DATABASE_URL}",
      "NEXTAUTH_SECRET": "${NEXTAUTH_SECRET}"
    }
  },
  "git": {
    "deploymentEnabled": {
      "main": true,
      "development": true,
      "preview": true
    }
  }
}